# homebrew-doumi

Homebrew cask for [Doumi](https://github.com/xirtus/doumi) — the open-source file automation assistant for macOS.

## Usage

```bash
brew install --cask xirtus/doumi/doumi
```

Or:

```bash
brew tap xirtus/doumi
brew install --cask doumi
```
